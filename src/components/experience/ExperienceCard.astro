---
interface Props {
  id: string;
  role: string;
  company: string;
  date: string;
  dateKey?: string;
  isActive: boolean;
}

const { id, role, company, date, dateKey, isActive } = Astro.props;
---

<div id={id} class={`experience-content ${isActive ? "" : "hidden"} px-5 ml-5`}>
  <h3 class="text-medium font-bold">
    {role} - <span class="text-secondary">{company}</span>
  </h3>
  <p class="text-gray-600 text-normal" data-i18n={dateKey}>{date}</p>
  <p class="text-normal text-dark leading-7 mb-4 mt-4">
    <slot />
  </p>
</div>
